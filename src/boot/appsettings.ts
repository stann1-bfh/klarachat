/**
 * Defines globally relevant static variables
 */
export class AppSettings {
    //URL of the Klarabot API
    static readonly HTTP_KLARABOT_API_ADDRESS = 'http://127.0.0.1:5000/';
    //Number of messages reladed per Scroll in the ChatHistory
    static readonly LOADING_INTERVAL = 5;
}